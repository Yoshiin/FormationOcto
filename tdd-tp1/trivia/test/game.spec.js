require('../src/game.js');

/*describe("The test environment", function() {
  it("should pass", function() {
    expect(true).toBe(true);
  });

  it("should access game", function() {
    expect(Game).toBeDefined();
  });
});

describe("Run a game", () => {
  it("should add a single player which should answer correctly", () => {
    const g = new Game();
    g.add("Player One");
    g.roll(6);
    expect(g.wasCorrectlyAnswered()).toBeTruthy();
  });

  it("should add a single player which should answer falsly", () => {
    const g = new Game();
    g.add("Player One");
    g.roll(6);
    expect(g.wrongAnswer()).toBeTruthy();
  });

});*/
